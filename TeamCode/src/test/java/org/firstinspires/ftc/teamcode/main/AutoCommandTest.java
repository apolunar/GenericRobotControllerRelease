package org.firstinspires.ftc.teamcode.main;

import android.util.Log;

import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.robolectric.RobolectricTestRunner;
import org.robolectric.annotation.Config;
import org.robolectric.shadows.ShadowLog;

@RunWith(RobolectricTestRunner.class)
public class AutoCommandTest {
    @Test
    public void testNextGameScorer() {
        // TODO: Figure out how to write a test for this!
//        GameScorer nextGameScorer = AutoCommand.getNextGameScorer(3, 0, 0, new Pose2d(0, 0, new Rotation2d(0)), GameEngine.GameStage.OPENING);
//        log("Best Game Scorer: " + nextGameScorer.getClass().getName());
    }
}
